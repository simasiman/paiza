SELECT division.division_name,COUNT(*)
 FROM member JOIN division
 ON member.division_id=division.division_id
 WHERE member.division_id=division.division_id
 GROUP BY member.division_id
 ORDER BY member.division_id;
